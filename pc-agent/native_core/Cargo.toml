[package]
name = "native_core"
version = "0.1.0"
edition = "2024"

[lib]
name = "native_core"
crate-type = ["cdylib"]

[dependencies]
regex = "1.10.5"
once_cell = "1.19.0"
pyo3 = { version = "0.22.2", features = ["extension-module"] }

